/* para ejecutar jquery o javascript en html es con la etiqueta scrip */
    $(document).ready(function(){
      $('#menuContainer').load('/componentes/footer.html');
      $('#footerContainer').load('/componentes/footer.html');
    });