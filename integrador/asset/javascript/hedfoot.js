/* para ejecutar jquery o javascript en html es con la etiqueta scrip */
    $(document).ready(function(){
      $('#menuContainer').load('/layout/heder.html');
      $('#footerContainer').load('/layout/footer.html');
    });